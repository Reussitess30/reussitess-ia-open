// const { Configuration, OpenAIApi } = require("openai");

// Ajoutez ici une exportation vide ou factice pour éviter une erreur
module.exports = {
  // Ajoutez vos exportations factices ici si nécessaire pour le reste du bot
};
